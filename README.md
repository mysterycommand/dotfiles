# Dotfiles

Setup:

1. [Install `oh my zsh`](https://ohmyz.sh/#install)
2. [Install `brew`](https://brew.sh/#install)
3. Install stuff with `brew`:
   - `brew install fnm rust go coreutils git`
4. Install stuff with `fnm`:
   - `fnm install --lts`
   - `fnm default lts-latest`
   - `npm -g i npm` (get latest `npm`)
5. [Install Deno](https://docs.deno.com/runtime/getting_started/installation/)
